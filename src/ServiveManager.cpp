#include "ServiceManager.h"

namespace amoureux
{
    // 注册服务节点（若已存在则忽略）
        void Register(const std::string &service, const Endpoint &endpoint)
        {

        }
        // 取消注册指定节点
        void Unregister(const std::string &service, const Endpoint &endpoint)
        {
            
        }
}